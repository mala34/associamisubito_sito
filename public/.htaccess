RewriteEngine On

# Force HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [L,R=301]

# Redirect www to non-www
RewriteCond %{HTTP_HOST} ^www\.associamisubito\.it [NC]
RewriteRule ^(.*)$ https://associamisubito.it/$1 [L,R=301]
